::  Write a switch statement using a `?` wut rune which branches on a type union of `$(%one %two ...)` up to five, and returns the `@ud` equivalent in a unit.  If the number is not present, the expression should return ~. 
|=  type=@tas
?+  type  ~       
    %one  [~ 1]
    %two  [~ 2]
    %three  [~ 3]
    %four  [~ 4]
    %five  [~ 5]
==